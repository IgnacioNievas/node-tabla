# Notas

Este es mi primer progrma en Node

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Es la base para tabla de multiplicar
                                                [number] [required] [default: 5]
  -l, --listar   Muestra la tabla de multiplicar en consola
                                                      [boolean] [default: false]
  -h, --hasta    Hasta que numero multiplica la tabla     [number] [default: 10]
```
